# skillmatcher/db/base.py
from sqlmodel import SQLModel

# Import models here (so metadata sees them)
# from skillmatcher.db.models import User, Job, Resume  # not needed if other modules import models
